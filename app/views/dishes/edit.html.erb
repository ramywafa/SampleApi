<%= page_header("Edit #{@dish.name}") %>

<div class="col-lg-12">
  <div class="panel panel-default">
    <div class="panel-heading">
      Edit <%= @dish.name %>
    </div>
    <div class="panel-body">
      <%= render partial: 'form', locals: { submit_text: 'Update' } %>
    </div>
  </div>
</div>
